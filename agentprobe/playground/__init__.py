"""Playground: describe an agent (prompt + simulated tools) in a web page, inject a failure, and see how a
real model behaves. Runs locally; no user code is executed, tools only return the results you define."""
